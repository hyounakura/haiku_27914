.haiku-user-top
  = link_to root_path, class: "mypage" do
    = icon('fa', 'home')
  .haiku-user-top__user-name
    %span= @user.name
    %span>さんの俳句一覧
.haiku-middle
  = render partial: 'google-map', locals: { haikus: @haikus }
.haiku-btn